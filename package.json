{
  "name": "ocr-platform",
  "version": "1.0.0",
  "description": "OCR Evaluation and PDF Viewing Platform (FastAPI)",
  "type": "module",
  "scripts": {
    "server": "uv run python -m uvicorn server.main:app --host 0.0.0.0 --port 3050 --reload",
    "test": "uv run python tests/test_ocr.py",
    "test:api": "uv run pytest tests/test_api.py"
  }
}
